<%= render 'header' %>

<div class="wrapper">
  <div class="banner">

    <div class="popup">
      <% if current_user %>
        <h1 class="h1">HOLD <span class="highlight">$ANIMEBNB</span>, <span class="pull-right">EARN BNB.</span></h1>
      <h2 class="center">Thanks for signing up!<br /> Stay tuned for updates!</h2>
      <img src="https://pa1.narvii.com/7676/717445cd850127611f2ee15491fd9711df870c2er1-520-293_hq.gif" />
      <br  />

      <% else %>
      <h1 class="h1">HOLD <span class="highlight">$ANIMEBNB</span>, <span class="pull-right">EARN BNB.</span></h1>
      <p>
        AnimeBNB  <span class="highlight">$ANB</span> is the  world's first Auto reward BNB cryptocurrency token.
      </p>
      <p>
         At Anime BNB we are dedicated to giving the community a unique rewards system with exclusive payouts through a gaming platform so you can play and earn $BNB and <span class="highlight">$ANB</span>.
    </p>
      <p>
         Presale Started. Join telegram for BNB giveaways
      </p>
      <%= link_to user_google_oauth2_omniauth_authorize_path, class: "login_link button", method: :post do %>
      Sign up for community updates
      <%#= image_tag("google_login.png", class: "google_login")%>
      <% end %>

      <% end %>
        <a href="https://dxsale.app/app/v2_9/defipresale?saleID=2030&chain=BSC" target="_blank" class="presale_link button secondary">
        <div id="timer">
          Click here to buy $ANB
        </div>

      <span>Join the presale</span>

      </a>
    </div>

  </div>


  <% if user_signed_in? %>
  <div class="profile_widget">
    <a id="myLink" title="Click to do something"  onclick="myFunction()">
      <%= image_tag("profile_photo.png", class: "avatar")%>
    </a>

    <div id="profile_menu" class="profile_menu hidden">
      <ul>
        <li class="list_header">
          <%= @user.first_name %>
          <span class="wallet">45¥</span>
        </li>
        <li>
          Edit Profile
        </li>
        <li>
          <%= link_to('Logout', destroy_user_session_path, method: :delete) %>
        </li>
      </ul>
    </div>

  </div>
  <% end %>
  </div>
  <script>
    document.addEventListener("turbolinks:before-cache", function() {
    $(document).ready(function(){
      $('[data-toggle="tooltip"]').tooltip();
    });
  });
  </script>
  <script>
    function myFunction() {
      var menu_element =  document.getElementById("profile_menu");
      menu_element.classList.toggle('hidden');
    }
  </script>
<script>
  const finaleDate = new Date("August 3, 2021 00:08:00").getTime();

  const timer = () =>{
      const now = new Date().getTime();
      let diff = finaleDate - now;
      // Showing the alert when the counter time finishes.
      if(diff < 0){
          document.querySelector('.alert').style.display = 'block';
          document.querySelector('.container').style.display = 'none';
      }

      let days = Math.floor(diff / (1000*60*60*24));
      let hours = Math.floor(diff % (1000*60*60*24) / (1000*60*60));
      let minutes = Math.floor(diff % (1000*60*60)/ (1000*60));
      let seconds = Math.floor(diff % (1000*60) / 1000);
    // Adding the zeros.
      days <= 99 ? days = `${days}` : days;
      days <= 9 ? days = `${days}` : days;
      hours <= 9 ? hours = `0${hours}` : hours;
      minutes <= 9 ? minutes = `0${minutes}` : minutes;
      seconds <= 9 ? seconds = `0${seconds}` : seconds;

      document.querySelector('#days').textContent = days;
      document.querySelector('#hours').textContent = hours;
      document.querySelector('#minutes').textContent = minutes;
      document.querySelector('#seconds').textContent = seconds;

  };
  timer();
  // Calling the function every 1000 milliseconds.
  setInterval(timer,1000);
</script>
